import { ExtractSummaryPipe } from './extract-summary.pipe';

describe('ExtractSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
