import { RemoveMinusPipe } from './remove-minus.pipe';

describe('RemoveMinusPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveMinusPipe();
    expect(pipe).toBeTruthy();
  });
});
