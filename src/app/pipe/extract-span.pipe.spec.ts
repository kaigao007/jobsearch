import { ExtractSpanPipe } from './extract-span.pipe';

describe('ExtractSpanPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractSpanPipe();
    expect(pipe).toBeTruthy();
  });
});
